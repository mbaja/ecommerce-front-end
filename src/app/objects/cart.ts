export class Cart {
  ItemID:number;     // INT UNSIGNED NOT NULL,
  CustomerID:string; // VARCHAR(50) NOT NULL,
  Quantity:number;   // INT UNSIGNED NOT NULL,
  CartID:number;      // INT UNSIGNED NOT NULL AUTO_INCREMENT,
}
